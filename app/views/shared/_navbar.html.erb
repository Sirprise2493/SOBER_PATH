<nav class="navbar">
  <div class="navbar-left">
    <span class="navbar-logo">SoberPath</span>
  </div>
  <div class="navbar-right">
    <a href="root_path">Home</a>
    <a href="#">About</a>
    <a href="#">Stories</a>

    <% if user_signed_in? %>
      <%= button_to "Log out",
                    destroy_user_session_path,
                    method: :delete,
                    class: "navbar-btn logout-btn" %>
    <% else %>
      <%= link_to "Log in", new_user_session_path, class: "navbar-btn login-btn" %>
      <%= link_to "Sign up", new_user_registration_path, class: "navbar-btn signup-btn" %>
    <% end %>
  </div>
</nav>
